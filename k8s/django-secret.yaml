# EXAMPLE ONLY. Provision real values with kubectl; do not apply this file.
apiVersion: v1
kind: Secret
metadata:
  name: django-secret
  namespace: wrapzy
type: Opaque
stringData:
  DJANGO_SECRET_KEY: "REPLACE_WITH_PRIVATE_SIGNING_KEY"
  ADMIN_USERNAME: "REPLACE_WITH_ADMIN_USERNAME"
  ADMIN_PASSWORD: "REPLACE_WITH_PRIVATE_ADMIN_PASSWORD"
